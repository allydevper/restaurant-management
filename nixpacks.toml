[phases.setup]
nixPkgs = ["nodejs", "caddy"]

[phases.build]
commands = ["npm install", "npm run build", "mv dist/restaurant-management /app/dist"]

[phases.start]
commands = ["caddy run --config /app/Caddyfile --adapter caddyfile"]
