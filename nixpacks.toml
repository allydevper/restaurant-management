[phases.setup]
nixPkgs = ["nodejs-18", "caddy"]

[phases.build]
commands = ["npm ci", "npm run build", "mv dist/restaurant-management/browser /app/dist"]

[phases.start]
commands = ["caddy run --config /app/Caddyfile --adapter caddyfile"]
